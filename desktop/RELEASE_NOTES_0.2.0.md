# B-JOB Desktop 0.2.0

- Offline-first Electron runtime.
- Local IndexedDB remains available without internet.
- LAN host/client transport with token authentication.
- Persistent device ID and sync journal.
- Duplicate operation protection.
- Backup JSON with SHA-256 manifest.
- Import verifies a manifest when it is present.
- Final manual verification checklist in `FINAL_TEST.md`.

## Not claimed as complete

Automatic background conflict resolution and live WB API synchronization are intentionally not claimed as complete until their rules and real API credentials are connected and tested against production-compatible data.
